# Local preview only. GitHub Pages builds the site server-side and ignores
# this file, so it never needs to match their toolchain exactly.
#
#   bundle install
#   bundle exec jekyll serve --drafts --livereload

source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

gem "webrick", "~> 1.8"

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
end
